# Execution-review — #1852 "N LANES LIVE" false positive (board consumes per-agentId pipeline punch-out)

Reviewer role: execution-review (independent — did NOT author the plan, the plan-review, or the code). Session `ee426cae`. Ticket #1852. Repo: agent-kanban. PR #64, branch `1852-board-live-false-positive`, base `master`.

cairn: `node skills/cairn/bin/cairn-find.mjs "board lane liveness chainInFlight"` (run from the ai-brain repo root) → `[WM 1852-punch-clock-stream-pollution.md:11] "The #1852 r2 mechanism (board consumes per-role closedAt punch-outs) is the RIGHT shape, but it is NOT sufficient to retire the liveness bug family until the punch-clock STREAM it consumes is fixed"` and `[T1 2026-07-24] "A ledger-mtime-based 'is this lane still alive' liveness check can false-fire on…"` — both quoted accurately, corroborate the plan's #1858-stream-dependency framing.

I verified every Binary AC against the actual diff and by re-running the tests/smokes myself — not against the PR body's prose.

## What I ran (command → actual result)

- **CI status** — `gh pr checks 64`: build (ubuntu-20) PASS, build (windows-20) PASS, privacy PASS, Vercel PASS. `gh pr view 64`: `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. Genuinely green.
- **Typecheck** — `npm run typecheck` (`tsc --noEmit`): clean, exit 0.
- **Full suite** — `npm test`: **Test Suites: 1 failed, 40 passed, 41 total; Tests: 382 passed, 382 total.** The single failing suite is `lane-reveal.test.ts`, failing on "Test environment jest-environment-jsdom cannot be found" — a missing-install/environment error, not an assertion failure. Verified pre-existing and unrelated: the diff touches neither `package.json` nor any jest config (confirmed `git diff --stat -- package.json jest.config.*` is empty), `origin/master`'s `package.json` already lists `jest-environment-jsdom` as a devDependency, and `lane-reveal.test.ts` itself is not in the diff. The "382/382, 1 pre-existing jsdom failure" claim is TRUE.
- **AC-0 real-hook smoke** — `bash scripts/ac0-real-hook-production-spawn-smoke.sh`: **ALL PASS, exit 0.** Drove the REAL ai-brain writer hooks (`three-role-spawn-ledger.sh` + `three-role-subagent-ledger.sh`) against the sibling ai-brain checkout. Mid-run (open punch-in, no SubagentStop) → live-count **1**; after the real SubagentStop overlay-merged `closedAt` onto the SAME row (one line, no zombie duplicate) → live-count **0**. Also proved the dispatch-time open row does NOT satisfy `gate-plan-review` (rc=2, no completion-gate regression — plan-review r3 note N1). This is a genuine Rule-18 measurement, not a fixture assertion, and it confirms AC-6 needs NO ai-brain change in the production (background) spawn mode.
- **AC-10 cross-repo outcome smoke** — `bash scripts/ac10-cross-repo-outcome-smoke.sh`: **HONEST PARTIAL, exit 0.** Ran the REAL `reconcile-spawns` sweep + REAL exporter over the three measured zombie shapes. Result `{"liveCount":2,"t1497":true,"t1682":false,"t1696":true}`: #1682 (ghost) **DARK** — closed by this PR alone (AC-9); #1497 (backfilled-open, never closed) and #1696 (killed mid-turn, sweep re-arms cap) stay **RED** pending #1858. This is the honest split the plan promised, not a fabricated all-green — the RED is the anti-phantom-forward-reference proof that #1858 is a real, unmet dependency.
- **AC-1 red-on-baseline (independent)** — checked out `origin/master`'s `lib/active.ts`, ran the rewritten AC-1 test against it: it **FAILS at line 112** (`chainInFlight(underTest)` returns `true`; test expects `false`) because master's pipeline branch ignores `closedAt`. Restored the branch file clean (empty diffstat). The red-on-baseline claim is REAL — the assertion genuinely appears only with the fix.
- **AC-8 red-against-partial (independent adversarial)** — temporarily reverted ONLY the `&& !hasAnyPipelineComment(focus)` conjunct (a disjunct-1-only partial fix) and re-ran the AC-8 suite: the sole-dead-chain test **FLIPS to FAIL at line 81** (`active.has("dead")` becomes `true` via the un-narrowed `inFlightIds.size === 0` grant). Restored clean. AC-8 genuinely forces the disjunct-2 narrowing — it is NOT an "AC that cannot fail" (the defect class this repo is scarred by).
- **r3 core suites targeted** — `lane-inflight-undercount`, `lane-disjunct2-focus-residual`, `lane-ghost-agentid-dedup`, `lane-punchout-cap-immunity`, `lane-role-handoff-gap`, `lane-punchout-exporter`, `research-inflight-lane`: **7 passed, 37 tests, all green.**
- **Scope** — `git diff origin/master...HEAD --name-only`: 9 files, all agent-kanban (`lib/active.ts`, six `__tests__/*.test.ts`, two `scripts/*.sh`). NO ai-brain / hooks / sole-lane files touched. The out-of-scope surfaces (#1858 writer, #1859 gate) are genuinely untouched.

## AC-by-AC verdict (checked against real code, not the PR prose)

- **AC-9 (per-agentId dedup) — MET, real not cosmetic.** `lib/active.ts` `pipelineHasOpenPunchIn` builds `agentId → hasAnyClosedRow` by OR-accumulating (`alreadyClosed || !!c.closedAt`) across all rows, then returns true only for an agent with ≥1 row and NO `closedAt` row. The measured #1682 ghost (open row + later closed row, same agentId) reads punched-OUT. Confirmed by the AC-9 suite (ghost dark; open-row-only live; delete-the-input flips it live; monotonicity test with the closed row appended first still dark; reopen with a fresh agentId stays live).
- **AC-8 (disjunct-2 FOCUS conjunct) — MET, correctly scoped.** The grant changed from `inFlightIds.size === 0 || inFlightIds.has(focus.id)` to `inFlightIds.has(focus.id) || (inFlightIds.size === 0 && !hasAnyPipelineComment(focus))`. `hasAnyPipelineComment` is deliberately punch-out-BLIND, so a punched-out dead chain (has pipeline comments) loses the unconditional grant while a genuinely-open focus chain stays lit via the unchanged `inFlightIds.has(focus.id)` (disjunct-1). Verified real by the red-against-partial run above.
- **AC-1 (rewrite, red-on-baseline) — MET.** Fails on master, passes on branch, with a delete-the-input oracle (clear one role's `closedAt` → flips active). Verified by hand above.
- **AC-0 / AC-10 — MET.** Both smokes drive the REAL hooks/exporter and pass for real (AC-0 all-pass; AC-10 honest partial). Verified by running them.
- **AC-2/3/4/5/7 — MET.** All present-and-green in the full and targeted runs (cap immunity, role-handoff-gap dark-between-roles + no-grace-window, exporter end-to-end one-live-lane).
- **Pre-existing chain-completeness path — preserved.** `if (!incompleteByState) return false;` short-circuits a PASS-completed chain BEFORE the punch-in check, so punch-out consumption never overrides a genuine PASS (mirrors pre-#1852 darkening).

## Monotonicity checklist (#1590) — every arm the diff introduces

- **AC-9 per-agentId dedup (open vs closed, same agentId).** Stronger claim = punched-OUT. The OR-accumulation is order-independent — ANY `closedAt` row wins, and a later open-looking row for the same agentId can never un-punch it (unit-proven by the monotonicity test). A genuine reopen mints a FRESH agentId, so a stale `closedAt` cannot erase a real reopen (unit-proven by the reopen test). Weaker cannot erase stronger. SAFE.
- **AC-8 disjunct-2 narrowing (chain-state evidence vs unconditional focus grant).** Stronger claim = "this focus chain is dead/punched-out." The new condition is strictly NARROWER than the old on the `size === 0` branch; the `inFlightIds.has(focus.id)` branch is preserved verbatim, so it can only turn OFF the dead-chain case, never turn ON a new one. The weaker "happens to be max-updatedAt" cannot relight a punched-out chain. SAFE.
- **Pipeline-branch punch-out consumption (spawn punch-in vs SubagentStop `closedAt`).** Stronger claim = `closedAt`. AC-0 smoke step 3 proves the SubagentStop hook overlay-merges `closedAt` onto the same row (one line, not a duplicate), so a late spawn-edge write cannot clear a `closedAt`. And `incompleteByState` gates the punch-in read so a PASS never gets overridden. Stronger done-claim preserved. SAFE.
- **Unchanged `chainInFlight` latch arms.** `newestExecReview` = LAST review, so an earlier PASS cannot erase a later FAIL; #1791 clause unchanged. SAFE.

## Notes (non-blocking)

- The AC-8 power-proof-against-partial is documented in the test file as "verified by hand" rather than encoded as a competing code path (which would be dead code duplicating `lib/active.ts`). I re-ran that hand verification myself (revert-the-conjunct → RED) and it holds — so the documented claim is honest, not hand-waved.
- Honest post-ship framing: the PR body correctly records `outcome_eval: partial` (board consumes punch-outs; #1682 ghost + disjunct-2 residual closed; #1497/#1696 still lit pending the separately-filed #1858), with #1858/#1859 as untouched, already-filed tickets. This matches AC-10's measured split exactly. Do NOT record `achieved` for #1852-alone.

## Review

Decision: PASS

Every Binary AC in the approved r3 plan is genuinely met against the actual diff, and every claimed test/smoke result reproduces independently: CI is truly green (MERGEABLE/CLEAN), typecheck is clean, the suite is 382/382 with the one `lane-reveal.test.ts` failure confirmed pre-existing (missing jsdom env, diff untouched), AC-0 and AC-10 pass for real against the live ai-brain hooks (honest partial, not a fabricated all-green), AC-1 is genuinely red on `origin/master`, and AC-8 is genuinely red against a disjunct-1-only partial fix (not an "AC that cannot fail"). The per-agentId dedup (AC-9) and the disjunct-2 conjunct (AC-8) are real, correctly scoped, and monotone-safe (#1590). The diff is agent-kanban-only — #1858/#1859 are honestly deferred and untouched. This PR is SAFE TO MERGE AS-IS (squash into master); no further ai-brain-side work is required for THIS ticket, and #1858/#1859 remain separately tracked.
